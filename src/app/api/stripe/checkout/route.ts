/**
 * POST /api/stripe/checkout
 * Creates a Stripe Checkout session for the Pro plan.
 * Alias of /api/billing/checkout — kept for canonical REST path.
 */
export { POST } from '@/app/api/billing/checkout/route'
