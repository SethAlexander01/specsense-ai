/**
 * POST /api/stripe/portal
 * Opens the Stripe Customer Portal for managing an existing subscription.
 * Alias of /api/billing/portal — kept for canonical REST path.
 */
export { POST } from '@/app/api/billing/portal/route'
